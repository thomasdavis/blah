BLAH - Barely Logical Agent Host

npm for mcp

# valtown

# slop

# agents.json

```
{
  "mcpServers": {
    "blah": {
      "command": "npx",
      "args": ["-y" , "blah-mcp"],
      "env": {}
    }
  }
}
```
